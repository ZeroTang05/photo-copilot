# SDK 选择 openai 或 anthropic。
AI_SDK=openai

# 所有 SDK 使用同一组变量。AI_BASE_URL 留空时使用所选 SDK 的官方地址。
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=gpt-4o-mini

SESSION_SECRET=replace-with-a-long-random-secret
# SESSION_SECRET 用于签名会话 cookie。若留空,启动时会自动生成进程级随机值。
# 正式部署请固定此值,否则每次重启会让所有现有会话失效。
ALLOWED_ORIGIN=http://localhost:5173
DAILY_AI_ATTEMPT_LIMIT=300
DAILY_AI_ATTEMPT_LIMIT_PER_SESSION=30

# 本地网关默认保存到 logs/ai-workflow.ndjson；生产环境使用平台运行日志。
# 如需自定义本地日志位置，填写绝对路径或相对项目根目录的路径。
AI_WORKFLOW_LOG_FILE=

# SAM3 文字分割。令牌只保存在服务端；留空时使用公开匿名连接。
# SAM_HF_TOKEN 支持多个令牌：用英文逗号分隔（如 hf_aaa,hf_bbb）组成令牌池，
# 某个令牌额度用尽时自动切换到下一个令牌重试。
SAM_PROVIDER=hf-gradio
SAM_SPACE_URL=https://prithivmlmods-sam3-demo.hf.space
SAM_HF_TOKEN=
SAM_TIMEOUT_MS=75000
# 公开 SAM3 Demo 尚未提供点坐标与独立蒙版输出，保持 false。
# 自建或获授权的提供方实现 /segment_points 后才开启。
SAM_POINT_SEGMENTATION=false
SAM_POINT_ENDPOINT=/segment_points
DAILY_SAM_ATTEMPT_LIMIT=60
DAILY_SAM_ATTEMPT_LIMIT_PER_SESSION=6

# 最终兜底：Hugging Face 令牌额度全部用尽或服务不可用时，改用你部署的 ModelScope
# Gradio API（较慢但无限量）。地址必须填 API 专用地址（形如
# https://studio-xxx.api-inference.modelscope.net），不是 Studio 网页地址。
# SAM_MODELSCOPE_TOKEN 填 ModelScope 访问令牌（模型仓库页 Access Tokens 获取）。
# 两项都留空则禁用兜底。SAM_MODELSCOPE_TIMEOUT_MS 是兜底端点单次调用超时（毫秒）。
SAM_MODELSCOPE_URL=
SAM_MODELSCOPE_TOKEN=
SAM_MODELSCOPE_TIMEOUT_MS=120000
