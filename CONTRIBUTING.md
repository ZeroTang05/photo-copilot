# 贡献指南

## 开始前

先查看现有 Issue，避免重复工作。功能较大的改动建议先创建 Issue 说明目标和使用场景。

## 提交要求

- 保持一次 Pull Request 聚焦一个目标
- 为可观察的行为变化补充测试
- 运行 `pnpm test` 和 `pnpm build:deploy`
- 在 Pull Request 中说明用户能看到的变化
- 不提交 API Key、`.env`、构建产物或测试缓存

## 设计原则

照片编辑操作应当可预览、可撤销并清楚表达结果。AI 建议应当保持为可选择的候选方案。

## 代码审查

每个 Pull Request 都会运行自动检查。维护者会关注编辑体验、兼容性、测试覆盖和安全影响。
